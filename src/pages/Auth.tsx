import LandingPage from "./LandingPage";

export default function AuthPage() {
  return (
    <div className="min-h-screen flex items-center justify-center bg-black text-green-400">
      <h1 className="text-4xl font-bold">🔒 Access Restricted</h1>
      <p className="mt-4 text-lg">
        Authenticate to unlock the neural pathways of innovation. Only the worthy
        may proceed.
      </p>
    </div>
  );
}